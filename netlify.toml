# Netlify deployment configuration

[build]
  publish = "."

# Redirect all routes to index.html for SPA behavior (optional)
[[redirects]]
  from = "/controller"
  to = "/controller.html"
  status = 200
